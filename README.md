# Jumper Docs

Documentation for Jumper's cross-chain experience. Built with [Mintlify](https://mintlify.com/) for fast iteration and deployment.

## Local preview

Install the Mintlify CLI to preview changes while you edit:

```bash
npm install -g mint
mint dev
```

Then open `http://localhost:3000`.

## Structure

- Guides (`index.mdx`, `quickstart.mdx`, `development.mdx`)
- Brand assets in `logo/` and `favicon.svg`

## Deployment

Push changes to the default branch to deploy. Keep `docs.json` in the repo root so Mintlify can discover the site configuration.

## Support

Reach the team at `support@jumper.exchange` for access keys, rate limits, or integration questions.
